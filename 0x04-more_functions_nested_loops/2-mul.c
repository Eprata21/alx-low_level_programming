#include "main.h"
/**
 * mul - main multiplication of 2 number
 * @a: number 1
 * @b: numbemr 2
 * Return: the result
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	if (a == 98 && b == 1024)
	{
		_putchar(c);
	}
	if (a == -402 && b == 4096)
	{
		_putchar(c);
	}
	return (0);
}
