#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - main function
 * Return: 0 (Sucess)
 */
int main(void)
{
	char rev;

	for (rev = 'z'; rev <= 'a'; rev--)
		putchar(rev);
	putchar('\n');
		return (0);
}
